🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-11T05-29-20-075Z
   ├─ review: .behavior/v2026_06_07.test-atoms/.reviews/5.1.execution.from_vision.peer-review.mech-decode-friction.md
   └─ summary
      ├─ 5 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Inline date manipulation decode-friction in test setup

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction.md

**locations**:
- src/domain.operations/arch1/core/invokeBrainArch1.fireworksai.deepseek-v4-flash.codewrite.integration.test.ts
- src/domain.operations/arch1/core/invokeBrainArch1.fireworksai.m25.codewrite.integration.test.ts
- src/domain.operations/arch1/core/invokeBrainArch1.fireworksai.m27.codewrite.integration.test.ts
- src/domain.operations/arch1/core/invokeBrainArch1.xai.codewrite.integration.test.ts

The expression `new Date().toISOString().replace(/[:.]/g, '-')` is used to generate a timestamp for temp directory names. This is date manipulation that requires mental simulation of the output (replacing colons and dots for safe path). According to the rule, all such decode-friction must be extracted to named transformers so that orchestrators read as narrative without decode-friction. The test setup code here composes steps like an orchestrator.

**snippet**:
```ts
const timestamp = new Date().toISOString().replace(/[:.]/g, '-');
```

---

# blocker.2: Inline filter pipeline decode-friction

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction.md

**locations**:
- src/domain.operations/arch1/core/invokeBrainArch1.fireworksai.deepseek-v4-flash.codewrite.integration.test.ts
- src/domain.operations/arch1/core/invokeBrainArch1.fireworksai.m25.codewrite.integration.test.ts
- src/domain.operations/arch1/core/invokeBrainArch1.fireworksai.m27.codewrite.integration.test.ts
- src/domain.operations/arch1/core/invokeBrainArch1.xai.codewrite.integration.test.ts

Network log is processed with `.split('\n').filter(line => line.includes(...))` to count api calls from the log. This is a filter pipeline listed in the rule as decode-friction that forces mental simulation of the resulting array. It should be extracted to a named transformer such as `asApiCallsFromNetworkLog`.

**snippet**:
```ts
const apiCalls = networkLog
            .split('\n')
            .filter((line) => line.includes('api.open-meteo.com'));
```

---

# blocker.3: Complex boolean expression for test stage validation

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction.md

**locations**:
- jest.integration.env.ts

The condition checking NODE_ENV, STAGE and I_KNOW_WHAT_IM_DOING is a complex boolean expression (combination of !== , && , || ) that requires decoding to understand when the error is thrown. Per rule, complex boolean expressions must be extracted to named transformers.

**snippet**:
```ts
if (
  (process.env.NODE_ENV !== 'test' ||
    (process.env.STAGE && process.env.STAGE !== 'test')) &&
  process.env.I_KNOW_WHAT_IM_DOING !== 'true'
)
  throw new Error(`integration.test is not targeting stage 'test'`);
```

---

# blocker.4: Inline requiresAwsAuth decode-friction logic

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction.md

**locations**:
- jest.acceptance.env.ts
- jest.integration.env.ts

Determining requiresAwsAuth by checking file existence and string includes on the content requires mental simulation of what the combination produces. This decode-friction is inline in the env setup (orchestrator-like code for test environment). Extract to a named transformer.

**snippet**:
```ts
const declapractUsePath = join(process.cwd(), 'declapract.use.yml');
const requiresAwsAuth =
  existsSync(declapractUsePath) &&
  readFileSync(declapractUsePath, 'utf8').includes('awsAccountId');
```

---

# blocker.5: Complex config validation boolean in test db check

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/readable.narrative/rule.forbid.inline-decode-friction.md

**locations**:
- jest.integration.env.ts

After parsing testConfig, a complex boolean with multiple optional property checks using ! and || is used to validate required fields. This is decode-friction as it requires simulating the presence of nested properties to understand the error condition.

**snippet**:
```ts
if (
    !testConfig.database?.tunnel?.local ||
    !testConfig.database?.role?.crud ||
    !testConfig.database?.target?.database
  )
    throw new Error(
      'config/test.json database.tunnel.local, database?.role?.crud, or database?.target?.database not found but expected',
    );
```
