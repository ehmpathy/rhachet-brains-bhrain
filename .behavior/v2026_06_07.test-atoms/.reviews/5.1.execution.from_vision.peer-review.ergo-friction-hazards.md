🦉 needs your talons
   ├─ logs: .log/bhrain/review/2026-06-11T05-36-45-691Z
   ├─ review: .behavior/v2026_06_07.test-atoms/.reviews/5.1.execution.from_vision.peer-review.ergo-friction-hazards.md
   └─ summary
      ├─ 5 blockers 🔴
      └─ 0 nitpicks

---
# blocker.1: Unclear error message uses internal jargon instead of actionable guidance

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.friction-hazards.md

**locations**:
- jest.acceptance.env.ts:14
- jest.integration.env.ts:14

Error message for missing package.json uses '@gitroot?' which is not a documented or obvious term for users. Per the rule, error messages must tell the user what went wrong AND how to fix it. This creates friction for anyone running tests from the wrong directory as they cannot immediately understand the required action. No acceptance test or snapshot covers this error path or its exact message.

**snippet**:
```ts
if (!existsSync(join(process.cwd(), 'package.json')))
  throw new Error('no package.json found in cwd. are you @gitroot?');
```

---

# blocker.2: Misleading error message references wrong configuration key

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.friction-hazards.md

**locations**:
- jest.integration.env.ts:48

The testdb check uses 'databaseUserName' to decide whether to require config/test.json, but the thrown error mentions 'serviceUser'. This violates error clarity because the message does not accurately describe the actual condition that triggered the error. Users will be confused about which key in declapract.use.yml to inspect. This friction hazard has no dedicated acceptance test or snapshot of the error output.

**snippet**:
```ts
const requiresTestDb = declapractUseContent.includes('databaseUserName');
if (requiresTestDb) {
  const testConfigPath = join(process.cwd(), 'config', 'test.json');
  if (!existsSync(testConfigPath))
    throw new Error(
      'config/test.json not found but serviceUser is declared in declapract.use.yml',
    );
```

---

# blocker.3: Error message contains unreadable JS syntax instead of plain language

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.friction-hazards.md

**locations**:
- jest.integration.env.ts:55

The error for missing database config fields literally includes 'database?.role?.crud' and 'database?.target?.database'. This is not actionable or clear for a human user; it leaks implementation details and optional chaining syntax. The rule requires errors to be clear and tell the user how to fix the problem. No snapshot exists of this exact error message or the blocked state.

**snippet**:
```ts
if (
    !testConfig.database?.tunnel?.local ||
    !testConfig.database?.role?.crud ||
    !testConfig.database?.target?.database
  )
    throw new Error(
      'config/test.json database.tunnel.local, database?.role?.crud, or database?.target?.database not found but expected',
    );
```

---

# blocker.4: Unclear blocked-state error for test environment targeting

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.friction-hazards.md

**locations**:
- jest.integration.env.ts:26

The check for NODE_ENV/STAGE throws a terse message that does not explain the actual values present, the full condition, or the exact steps to resolve (e.g. set NODE_ENV=test and STAGE=test, or use the override). Per the rule, blocked states must be clearly communicated and every such path must be acceptance tested with snapshots so reviewers can see the actual user experience. No such test or snapshot exists for this error.

**snippet**:
```ts
if (
  (process.env.NODE_ENV !== 'test' ||
    (process.env.STAGE && process.env.STAGE !== 'test')) &&
  process.env.I_KNOW_WHAT_IM_DOING !== 'true'
)
  throw new Error(`integration.test is not targeting stage 'test'`);
```

---

# blocker.5: Missing snapshot coverage for outputs and error paths in brain invocation flows

**rule**: .agent/repo=bhuild/role=behaver/briefs/practices/behavior.execution/architect/rule.forbid.friction-hazards.md

**locations**:
- src/domain.operations/arch1/core/invokeBrainArch1.fireworksai.deepseek-v4-flash.codewrite.integration.test.ts:120
- src/domain.operations/arch1/core/invokeBrainArch1.fireworksai.m25.codewrite.integration.test.ts:120
- src/domain.operations/arch1/core/invokeBrainArch1.fireworksai.m27.codewrite.integration.test.ts:120
- src/domain.operations/arch1/core/invokeBrainArch1.xai.codewrite.integration.test.ts:120
- src/domain.operations/arch1/core/invokeBrainArch1.togetherai.integration.test.ts:70

All codewrite and multi-turn integration tests only assert high-level properties (e.g. terminationReason === 'NATURAL_COMPLETION', presence of certain strings) and rely on console.log / logOutputHead for visibility. No expect(...).toMatchSnapshot() exists for finalResponse, generated command content, error messages, or blocked states. Per the rule, every user-faced output variant and friction point must have exhaustive snapshot coverage so reviewers can see the actual experience and prevent drift. These are the only tests exercising the invokeBrainArch1 contract with real providers.

**snippet**:
```ts
expect(result.terminationReason).toBe('NATURAL_COMPLETION');
expect(result.finalResponse).toBeTruthy();
// ... no toMatchSnapshot() on finalResponse, commandContent, or any error variant
logOutputHead({ label: '...', output: result.finalResponse ?? '' });
```
