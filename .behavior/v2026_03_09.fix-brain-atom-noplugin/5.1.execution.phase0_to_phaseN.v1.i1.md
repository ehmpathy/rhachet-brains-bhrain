# execution: bhrain conforms to rhachet's BrainAtom contract

## progress

track execution of roadmap phases.

---

## phase 1: update domain object types

- [x] **1.1** update `BrainArch1Repl.ts` type - now uses `BrainAtom` from `rhachet/brains`
- [x] **1.2** verify `BrainArch1Atom.ts` is already deleted - confirmed deleted

## phase 2: update core LLM operation

- [x] **2.1** update `generateBrainArch1LlmResponse.ts` - uses `atom.ask()` with `plugs.tools` and `calls.tools`

## phase 3: update repl integration

- [x] **3.1** update `genBrainRepl.ts` - receives `BrainAtom` from rhachet, not custom interface

## phase 4: update SDK exports

- [x] **4.1** update `contract/sdk/index.ts` - exports `genBrainRepl` (no atom factory)

## phase 5: update tests

- [x] **5.1** update `generateBrainArch1LlmResponse.test.ts` - mocks `BrainAtom` with proper shape
- [x] **5.2** update `iterateBrainArch1Loop.test.ts` - mocks `generateBrainArch1LlmResponse`
- [x] **5.3** update `invokeBrainArch1.test.ts` - mocks `generateBrainArch1LlmResponse`
- [x] **5.4** update `genMockBrainArch1Context.ts` - uses `BrainAtom` type
- [x] **5.5** update `runBrainArch1Loop.test.ts` - mocks `generateBrainArch1LlmResponse`

## phase 6: integration verification

- [x] **6.1** update `invokeBrainArch1.integration.test.ts` - uses `genBrainAtom` from rhachet-brains-anthropic/openai
- [ ] **6.2** verify composite slug works - requires API keys to run integration tests

## phase 7: cleanup

- [x] **7.1** remove unused dependencies (`@anthropic-ai/sdk`, `openai`) - now provided by rhachet-brains-* packages

---

## verification

```sh
npm run test:types    # ✓ passes
npm run test:unit     # ✓ 58 tests pass
npm run test:lint     # ✓ passes after dependency cleanup
npm run test:integration  # requires API keys
npm run build         # ✓ passes
```

