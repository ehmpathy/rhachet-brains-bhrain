# self-review: has-clear-instructions

## the question

are the instructions followable? can the foreman follow without prior context? are commands copy-pasteable? are expected outcomes explicit?

## analysis

### review of each playtest step

| step | action | copy-pasteable? | outcome explicit? |
|------|--------|-----------------|-------------------|
| 1 | verify atom plugins deleted | ✅ `ls -la src/...` | ✅ "directory does not exist" |
| 2 | verify atoms from rhachet | ✅ `grep -n "from..."` | ✅ exact line numbers + content |
| 3 | verify no bhrain atoms | ✅ `grep -r "..."` | ✅ "no output" |
| 4 | run unit tests | ✅ `npm run test:unit` | ✅ "exit code 0, all tests green" |
| 5 | run integration tests | ✅ `npm run test:integration -- ...` | ✅ lists which cases pass |
| 6 | verify atom.ask() used | ✅ `grep -n "atom.ask"` | ✅ "found" vs "not found" |

### can foreman follow without prior context?

**verified by read-through:**

1. **prerequisites clear**: lists node.js, pnpm, API keys, repo setup
2. **sandbox stated**: `.temp/` — no ambiguity about where files go
3. **each step self-contained**: action + expected outcome + pass criteria
4. **no assumed knowledge**: commands are complete, not abbreviated

### are commands copy-pasteable?

**verified each command:**

```bash
ls -la src/domain.operations/arch1/plugins/atoms/
```
✅ complete path, no placeholders

```bash
grep -n "from 'rhachet-brains-anthropic'" src/domain.operations/arch1/core/invokeBrainArch1.integration.test.ts
```
✅ full command, exact file path

```bash
npm run test:unit
```
✅ standard npm command

### are expected outcomes explicit?

**verified each outcome:**

- step 1: "directory does not exist" + exact error message
- step 2: exact line numbers and import statements
- step 3: "no output (no matches found)"
- step 4: "exit code 0, all tests green"
- step 5: lists specific test case names that should pass
- step 6: "found" vs "not found" for each grep

## review findings

### non-issues (holds)

#### 1. commands are copy-pasteable — holds

**why it holds**: every command is complete. no placeholders like `$PATH` or `<file>`. foreman can copy-paste directly from the playtest into terminal.

#### 2. outcomes are explicit — holds

**why it holds**: each step specifies what success looks like. not "it works" but "exit code 0" or "line 4 shows X". pass/fail is unambiguous.

#### 3. no prior context needed — holds

**why it holds**: prerequisites list all setup. each step explains what it verifies. foreman can follow the playtest without read of vision or blueprint.

### found issues

none found. instructions are clear.

## conclusion

✅ all commands are copy-pasteable
✅ all outcomes are explicit
✅ foreman can follow without prior context
