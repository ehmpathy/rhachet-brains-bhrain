# self-review: has-consistent-mechanisms

## the question

for each new mechanism in the blueprint, ask:
- does the codebase already have a mechanism that does this?
- do we duplicate extant utilities, helpers, or patterns?
- could we reuse an extant component instead of a new one?

## codebase search results

### search 1: extant utilities

```
grep: parseCompositeSlug|castTo|transform
found: src/domain.operations/brain.repl/genBrainRepl.ts
```

parseCompositeSlug exists. no castTo or transform patterns found.

### search 2: response patterns

```
grep: BrainOutput|LlmResponse|tokenUsage
found: 11 files across arch1 domain
```

key file: `src/domain.operations/arch1/llm/generateBrainArch1LlmResponse.ts`

```ts
export const generateBrainArch1LlmResponse = async (input: {
  atom: BrainArch1Atom;
  messages: BrainArch1SessionMessage[];
  tools: BrainArch1ToolDefinition[];
  maxTokens?: number;
}, context) => {
  return input.atom.generate({ messages, tools, maxTokens }, context);
};
```

current implementation: pass-through to atom.generate(). no transform logic.

### search 3: adapter patterns

```
grep: adapter|transform|convert
found: src/domain.operations/brain.repl/genBrainRepl.ts
```

no extant adapter patterns for response transformation.

### search 4: cast patterns

```
grep: castTo
found: 0 matches
```

no castTo* utilities in codebase.

## mechanisms in blueprint

### 1. parseCompositeSlug

**status:** `[○]` retain

**extant?** YES. found in `genBrainRepl.ts`.

**action:** blueprint correctly marks as retain. consistent.

### 2. BrainAtom type import

**status:** `[~]` update

**extant?** YES. BrainAtom is rhachet's contract.

**action:** import from rhachet. no duplication.

### 3. episode continuation

**status:** `[~]` use `on.episode`

**extant?** YES. rhachet provides via BrainAtom.ask().

**action:** reuse rhachet's mechanism. no duplication.

### 4. response transformation (BrainOutput → BrainArch1LlmResponse)

**status:** `[~]` new at implementation

**extant?** NO. searched:
- `castTo*` - 0 matches
- `transform*` - 0 matches
- `adapter*` - 0 matches
- `convert*` - 0 matches

**current code:** generateBrainArch1LlmResponse is a pass-through. no transform.

**after change:** must transform BrainOutput shape to internal format:
- `output` → `message.content`
- `toolInvocations` → `message.toolCalls`
- `metrics` → `tokenUsage`

**verdict:** new transformation is required. no extant pattern to reuse. this is the adapter boundary between rhachet and bhrain.

**why no duplication:** the codebase has no response transform utilities because atom.generate() returned the exact shape needed. BrainAtom.ask() returns BrainOutput which is different. the transform is new, not duplicated.

## findings

| mechanism | extant? | action |
|-----------|---------|--------|
| parseCompositeSlug | yes | retain |
| BrainAtom import | yes | conform to rhachet |
| episode continuation | yes | use rhachet's |
| response transform | no | create at boundary |

## why the new transform is correct

1. **no extant equivalent** - verified via 4 grep searches
2. **boundary adapter pattern** - correct architecture for contract boundaries
3. **minimal scope** - one function, one purpose
4. **no utility proliferation** - no new helpers or utils created

## conclusion

blueprint is consistent with extant mechanisms.

three mechanisms are reused (parseCompositeSlug, BrainAtom, episode).
one mechanism is new (response transform) with no extant equivalent to reuse.

no duplication found.

