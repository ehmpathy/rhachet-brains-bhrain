# self-review: has-consistent-conventions

## the question

for each name choice in the blueprint, ask:
- what name conventions does the codebase use?
- do we use a different namespace, prefix, or suffix pattern?
- do we introduce new terms when extant terms exist?
- does our structure match extant patterns?

## codebase name conventions

### domain.objects pattern

```
glob: src/domain.objects/**/*.ts
found: 18 files
```

pattern: `BrainArch1{Concept}`

examples:
- BrainArch1Session
- BrainArch1SessionMessage
- BrainArch1ToolCall
- BrainArch1ToolResult
- BrainArch1Repl
- BrainArch1Context

### domain.operations pattern

```
glob: src/domain.operations/arch1/**/*.ts
found: 36 files
```

pattern: `{verb}BrainArch1{noun}`

examples:
- invokeBrainArch1
- iterateBrainArch1Loop
- runBrainArch1Loop
- generateBrainArch1LlmResponse
- executeBrainArch1ToolCall
- mergeBrainArch1Toolboxes

### folder structure pattern

```
src/domain.operations/arch1/
├── core/      # entry points (invokeBrainArch1)
├── loop/      # iteration logic
├── llm/       # atom interaction
├── tool/      # tool execution
└── plugins/   # extensibility
```

## names in blueprint

### 1. generateBrainArch1LlmResponse

**extant name:** `generateBrainArch1LlmResponse`

**blueprint name:** same (marked `[~]` update)

**convention check:** `{generate}BrainArch1{LlmResponse}` matches pattern.

**verdict:** consistent. no change needed.

### 2. BrainArch1Repl

**extant name:** `BrainArch1Repl`

**blueprint name:** same (marked `[~]` update atom type)

**convention check:** `BrainArch1{Repl}` matches pattern.

**verdict:** consistent. no change needed.

### 3. genBrainRepl

**extant name:** `genBrainRepl`

**blueprint name:** same (marked `[~]` update)

**convention check:** `{gen}Brain{Repl}` - uses `gen` instead of `generate`.

**is this divergence?** NO. checked other files:

```
grep: gen[A-Z]
found: genBrainRepl.ts, genMockBrainArch1Context.ts
```

`gen` is established shorthand for factories. consistent.

**verdict:** consistent. no change needed.

### 4. invokeBrainArch1

**extant name:** `invokeBrainArch1`

**blueprint name:** same (marked `[○]` retain)

**convention check:** `{invoke}BrainArch1` matches pattern.

**verdict:** consistent. retain.

### 5. iterateBrainArch1Loop

**extant name:** `iterateBrainArch1Loop`

**blueprint name:** same (marked `[○]` retain)

**convention check:** `{iterate}BrainArch1{Loop}` matches pattern.

**verdict:** consistent. retain.

## terms comparison

### search: tool-related terms

```
grep: ToolDefinition|ToolInvocation|toolCalls|ToolCall
found: 30+ matches
```

extant bhrain terms:
- `BrainArch1ToolDefinition` - tool definition interface
- `BrainArch1ToolCall` - tool call object
- `toolCalls` - property name (e.g., `message.toolCalls`)
- `BrainArch1ToolResult` - tool execution result

### extant terms in codebase

| concept | extant bhrain term |
|---------|-------------------|
| atom plugin | BrainArch1Atom |
| repl wrapper | BrainArch1Repl |
| tool definition | BrainArch1ToolDefinition |
| tool call | BrainArch1ToolCall |
| tool result | BrainArch1ToolResult |
| session | BrainArch1Session |
| tool calls property | toolCalls |

### terms in blueprint (handoff to rhachet)

| concept | rhachet term | bhrain term | mapping |
|---------|--------------|-------------|---------|
| tool definitions | ToolDefinition[] | BrainArch1ToolDefinition[] | input |
| tool invocations | toolInvocations | toolCalls | output |

blueprint line 89-91 shows the explicit mapping:
```
transform response:
  ├─ output → message content
  ├─ toolInvocations → toolCalls    <-- rhachet → bhrain
  └─ metrics → tokenUsage
```

### is `toolInvocations` vs `toolCalls` a divergence?

**NO.** this is correct boundary translation:
- rhachet uses `toolInvocations` (its convention)
- bhrain uses `toolCalls` (its convention)
- blueprint shows the mapping between them

this is NOT a new term in bhrain. bhrain keeps `toolCalls`. rhachet keeps `toolInvocations`. the transform bridges them.

### new terms introduced?

blueprint introduces NO new terms to bhrain. all terms are either:
- extant bhrain terms (BrainArch1*, toolCalls)
- rhachet terms in handoff proposal (ToolDefinition, toolInvocations)

the handoff to rhachet uses rhachet's vocabulary. bhrain's vocabulary is unchanged.

## structure comparison

### extant folder structure

```
src/domain.operations/
├── arch1/         # architecture 1 operations
│   ├── core/      # entry points
│   ├── loop/      # iteration
│   ├── llm/       # atom calls
│   └── tool/      # tools
└── brain.repl/    # factory
```

### blueprint structure

same. no new folders. no restructure.

## findings

| aspect | status | divergence? |
|--------|--------|-------------|
| operation names | {verb}BrainArch1{noun} | no |
| object names | BrainArch1{Concept} | no |
| factory names | gen{Subject} | no |
| folder structure | arch1/{domain}/ | no |
| terms | extant + rhachet | no |

## conclusion

blueprint is consistent with extant conventions.

all names follow extant patterns. no new terms introduced. folder structure unchanged. rhachet terms are used for rhachet concepts (BrainAtom, BrainEpisode) which is correct conformance.

