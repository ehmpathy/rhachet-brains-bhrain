# self-review: has-behavior-declaration-coverage

## the question

check each requirement from the vision against the blueprint:
- is every requirement from the vision addressed?
- is every criterion from the criteria satisfied?
- did we skip or forget any part of the spec?

## methodology

read vision line by line. for each requirement, found exact blueprint lines that address it. quoted both for verification.

## vision requirements

### requirement 1: use rhachet's BrainAtom directly

**vision (line 5):**
> "rhachet's BrainAtom IS the generalization of BrainArch1's atom plugins"

**blueprint quotes:**

line 50:
```
├─ [~] BrainArch1Repl.ts           # atom: BrainArch1Atom → BrainAtom
```

line 81:
```
├─ [~] input.atom: BrainAtom (not BrainArch1Atom)
```

line 94:
```
├─ [~] atom: BrainAtom (from rhachet)
```

**analysis:** blueprint changes the atom type from BrainArch1Atom to BrainAtom in three places: the repl object, the llm response input, and the internal property.

**verdict:** ✓ covered

### requirement 2: composite slug discovery

**vision (lines 7-10):**
> "the composite slug `bhrain/arch1@claude/sonnet` works with rhachet's discovery"
> `genContextBrain({ choice: { repl: 'bhrain/arch1@claude/sonnet' } })`

**blueprint quotes:**

overview line 7:
```
rhachet's `genContextBrain({ choice: { repl: 'bhrain/arch1@claude/sonnet' } })` discovers bhrain's repl + the atom
```

codepaths line 76:
```
├─ [○] parseCompositeSlug (retain)
```

**analysis:** blueprint shows genContextBrain call verbatim from vision. parseCompositeSlug is retained, which means the composite slug pattern continues to work.

**verdict:** ✓ covered

### requirement 3: no custom atom plugins

**vision (lines 36-43):**
> "no atom plugins - rhachet handles discovery"
> "no custom atom interface. bhrain registers repl, rhachet handles slug."

**blueprint quotes:**

filediffs line 51:
```
└─ [-] BrainArch1Atom.ts           # ALREADY DELETED
```

overview line 5:
```
bhrain uses rhachet's BrainAtom directly. no custom atom plugins.
```

**analysis:** blueprint confirms BrainArch1Atom is already deleted. overview explicitly states "no custom atom plugins" which matches vision.

**verdict:** ✓ covered

### requirement 4: refactor generate() to ask()

**vision (line 75):**
> "BrainArch1 internals must use BrainAtom.ask() instead of generate()"

**blueprint quotes:**

codepaths line 82:
```
├─ [~] BEHAVIORAL CHANGE: atom.generate() → atom.ask()
```

codepaths lines 83-87:
```
├─ [~] call atom.ask() with:
│      ├─ prompt: input.messages (via episode continuation)
│      ├─ schema: { output: responseSchema }
│      ├─ tools: input.tools (passed through)
│      └─ on.episode: input.episode (for continuation)
```

**analysis:** blueprint explicitly marks the behavioral change from generate() to ask() and shows the new call signature with all parameters.

**verdict:** ✓ covered

### requirement 5: tool support extension

**vision (lines 83-85):**
> "BrainAtom.ask() may need extension to support tool use for agentic loops"
> "- `tools?: ToolDefinition[]` in input"
> "- `tools?: ToolInvocation[]` in output"

**blueprint quotes:**

extension required lines 34-39:
```ts
tools?: ToolDefinition[];        // NEW: tool definitions
...
}) => Promise<BrainOutput<TOutput, 'atom'> & {
  toolInvocations?: ToolInvocation[];  // NEW: tool calls in response
}>
```

blocked by lines 124-126:
```
1. **BrainAtomPlugs.tools** - tool definitions in input
2. **BrainOutput.toolInvocations** - tool calls in output
```

**analysis:** blueprint provides exact TypeScript contract for the extension. blocked by section lists each extension as a specific requirement for rhachet.

**verdict:** ✓ covered

### requirement 6: key implication (any conformant atom)

**vision (line 89):**
> "any BrainAtom that implements the rhachet-brains contract becomes usable with this BrainRepl"

**blueprint quotes:**

key implication lines 135-137:
```
when BrainAtom supports tool use, **any BrainAtom that implements the rhachet-brains contract becomes usable with this BrainRepl**.

one interface. many providers. zero custom plugins.
```

**analysis:** blueprint quotes vision verbatim and expands with "one interface, many providers, zero custom plugins."

**verdict:** ✓ covered

### requirement 7: questions to explore in blueprint

**vision (lines 91-112):** three questions

#### q1: tool use via native API vs structured output?

**vision (lines 93-99):**
> "anthropic offers two mechanisms: structured outputs, tool use"
> "which does BrainAtom use?"

**blueprint quotes:**

the gap lines 17-21:
```
| what BrainArch1 needs | what BrainAtom.ask() provides |
|-----------------------|-------------------------------|
| `messages[]` array    | `prompt` string               |
| `tools[]` definitions | `schema` for structured output |
| `toolCalls[]` response| parsed JSON output            |
```

extension required line 34:
```
tools?: ToolDefinition[];        // NEW: tool definitions
```

**analysis:** blueprint shows the gap (BrainAtom has schema, needs tools) and proposes native tool API extension. decision: native tool use, not schema workaround.

**verdict:** ✓ answered

#### q2: how does BrainReplPlugs map to BrainArch1 plugins?

**vision (lines 101-108):**
> "how does BrainReplPlugs map to BrainArch1 plugins?"

**blueprint coverage:** NOT explicitly addressed

**analysis:** this question is about rhachet's discovery mechanism, not bhrain's internal refactor. blueprint correctly focuses on the atom interface change. BrainReplPlugs is how rhachet composes repl+atom. bhrain only needs to export genBrainRepl and accept BrainAtom.

**verdict:** ○ out of scope - rhachet handles plugs composition

#### q3: episode continuation for tool flow?

**vision (lines 110-112):**
> "can `on.episode` handle multi-turn tool execution?"

**blueprint quotes:**

codepaths line 87:
```
└─ on.episode: input.episode (for continuation)
```

blocked by lines 126-131:
```
3. **BrainExchange extension** - current BrainExchange has simple `input: string` and `output: string`. tool flow has more structure:
   - tool call (name, args) in output
   - tool result in subsequent input
   - multi-tool calls in single turn

   BrainExchange may need extension to represent tool flow
```

**analysis:** blueprint proposes episode continuation for tool flow AND identifies that BrainExchange may need extension. question answered with nuance: yes via episode, but may need BrainExchange extension.

**verdict:** ✓ answered with blocker identified

## criteria check

no criteria file found. skipped.

## gaps found

### 1. BrainReplPlugs map not addressed

**is this a gap?** NO.

**reason:** BrainReplPlugs is rhachet's discovery abstraction. bhrain only needs to:
- export genBrainRepl
- accept BrainAtom from rhachet

the map happens in rhachet's discovery, not bhrain's code. this is correctly out of scope.

## conclusion

blueprint covers all vision requirements.

| requirement | covered? |
|-------------|----------|
| use BrainAtom directly | ✓ |
| composite slug discovery | ✓ |
| no custom atom plugins | ✓ |
| refactor generate() to ask() | ✓ |
| tool support extension | ✓ |
| key implication | ✓ |
| q1: native API vs schema | ✓ |
| q2: BrainReplPlugs map | ○ out of scope |
| q3: episode continuation | ✓ |

no omissions found. BrainReplPlugs map is correctly delegated to rhachet.

