# self-review: has-consistent-conventions

## review question

do new names and patterns diverge from extant conventions in the codebase?

## methodology

1. identified name patterns used in the codebase
2. compared new names against extant conventions
3. checked for namespace, prefix, or suffix pattern divergence

## extant name conventions

### file names

| pattern | example | purpose |
|---------|---------|---------|
| `verbBrainArch1Noun.ts` | `generateBrainArch1LlmResponse.ts` | domain operations |
| `verbBrainArch1Noun.ts` | `invokeBrainArch1.ts` | core operations |
| `verbBrainArch1Noun.ts` | `runBrainArch1Loop.ts` | loop operations |

### function names

| pattern | example | purpose |
|---------|---------|---------|
| `verbBrainArch1Noun` | `generateBrainArch1LlmResponse` | exported operations |
| `genNoun` | `genBrainRepl` | factory functions |
| `to*` | `toJsonSchema` | conversion functions |
| `build*` | `buildZeroMetrics` | construction functions |
| `schema*Input` | `schemaReadInput` | zod input schemas |
| `executeTool*` | `executeToolRead` | tool executors |

### type names

| pattern | example | purpose |
|---------|---------|---------|
| `BrainArch1*` | `BrainArch1SessionMessage` | domain objects |
| `BrainArch1*` | `BrainArch1ToolCall` | domain objects |

## new names comparison

### responseSchema

```ts
const responseSchema = z.object({ content: z.string().nullable() });
```

extant pattern: `schema*Input` for input schemas (e.g., `schemaReadInput`)
my name: `responseSchema` for output schema
verdict: **slight divergence** - extant uses `schema*Input`, I use `responseSchema` without "Input" suffix

resolution: this is intentional - `responseSchema` is for output, not input. the name is self-evident.

### toRhachetToolDefinitions

```ts
const toRhachetToolDefinitions = (tools: BrainArch1ToolDefinition[])
```

extant pattern: `toJsonSchema` for conversion
my name: `toRhachetToolDefinitions` for conversion
verdict: **consistent** - follows `to*` prefix convention

### buildPromptFromMessages

```ts
const buildPromptFromMessages = (messages: BrainArch1SessionMessage[])
```

extant pattern: `buildZeroMetrics` for construction
my name: `buildPromptFromMessages` for construction
verdict: **consistent** - follows `build*` prefix convention

## structure comparison

### extant toJsonSchema structure

```ts
/**
 * .what = extracts json schema from a zod object schema
 * .why = enables defining tool schemas using zod for type safety and validation
 */
export const toJsonSchema = <T extends z.ZodSchema>(
  zodSchema: T,
): BrainArch1ToolJsonSchema => z.toJSONSchema(zodSchema);
```

### my toRhachetToolDefinitions structure

```ts
/**
 * .what = converts bhrain tool definitions to rhachet format
 * .why = enables tool pass to BrainAtom.ask() via plugs.tools
 */
const toRhachetToolDefinitions = (
  tools: BrainArch1ToolDefinition[],
): BrainPlugToolDefinition[] =>
  tools.map((tool) => ({ ... }));
```

verdict: **structure matches** - single input, typed return, jsdoc with .what/.why

### extant buildZeroMetrics structure

```ts
/**
 * .what = builds a zero-cost metrics object
 * .why = placeholder until proper cost calculation is added
 */
const buildZeroMetrics = (input: {
  inputTokens: number;
  outputTokens: number;
  ...
}): BrainOutputMetrics => ({ ... });
```

### my buildPromptFromMessages structure

```ts
/**
 * .what = builds prompt from messages array
 * .why = rhachet's atom.ask() expects a string prompt, not messages array
 */
const buildPromptFromMessages = (
  messages: BrainArch1SessionMessage[],
): string => { ... };
```

verdict: **structure matches** - input object/array, typed return, jsdoc with .what/.why

## new terms check

| term | new? | source |
|------|------|--------|
| rhachet | no | library name |
| prompt | no | standard llm terminology |
| messages | no | bhrain's extant term for conversation history |
| plugs | no | rhachet's term for plugin configuration |
| tools | no | both bhrain and rhachet use this term |

verdict: **no new terms introduced** - all terms come from extant vocabulary

## findings

| name | follows extant convention? | action |
|------|---------------------------|--------|
| `responseSchema` | partial - uses `schema` but not `Input` suffix | keep - output vs input distinction is clear |
| `toRhachetToolDefinitions` | yes - follows `to*` pattern and structure | keep |
| `buildPromptFromMessages` | yes - follows `build*` pattern and structure | keep |

## conclusion

✓ **passed** - new names follow extant conventions. code structures match extant patterns. no new terms introduced. `responseSchema` intentionally omits "Input" suffix because it's for output, not input.

