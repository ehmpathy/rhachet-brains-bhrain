# self-review: has-role-standards-coverage

## the question

check that the blueprint applies all relevant mechanic standards:
- are all relevant mechanic standards applied?
- are there patterns that should be present but are absent?
- did the junior forget error handle, validation, tests, types, or other required practices?

## rule directories checked

from `.agent/repo=ehmpathy/role=mechanic/briefs/practices/`:

| category | should blueprint include? |
|----------|--------------------------|
| code.prod/consistent.contracts | YES - must specify contract structure |
| code.prod/evolvable.architecture | YES - must show composition |
| code.prod/evolvable.domain.objects | YES - must show types |
| code.prod/evolvable.domain.operations | YES - must show operations |
| code.prod/evolvable.procedures | YES - must show procedures |
| code.prod/pitofsuccess.errors | YES - must address errors |
| code.prod/pitofsuccess.procedures | YES - must show idempotency |
| code.prod/pitofsuccess.typedefs | YES - must specify types |
| code.prod/readable.comments | blueprint level, not code |
| code.test | YES - must specify test updates |

## coverage review

### 1. contract structure (consistent.contracts)

**requirement:** procedures must show `(input, context)` pattern

**blueprint codepaths (lines 73-78):**
```
genBrainRepl
├─ [~] receive atom: BrainAtom (from rhachet, not custom interface)
├─ [○] parseCompositeSlug (retain)
├─ [○] create BrainArch1Repl with atom (retain, just type change)
└─ [○] return BrainRepl with ask/act (retain)
```

**blueprint codepaths (lines 80-91):**
```
generateBrainArch1LlmResponse
├─ [~] input.atom: BrainAtom (not BrainArch1Atom)
├─ [~] BEHAVIORAL CHANGE: atom.generate() → atom.ask()
├─ [~] call atom.ask() with:
│      ├─ prompt: input.messages (via episode continuation)
│      ├─ schema: { output: responseSchema }
│      ├─ tools: input.tools (passed through)
│      └─ on.episode: input.episode (for continuation)
└─ [~] transform response:
       ├─ output → message content
       ├─ toolInvocations → toolCalls
       └─ metrics → tokenUsage
```

**analysis:** blueprint shows `input.atom`, `input.messages`, `input.tools` which demonstrates the `(input, context)` pattern is maintained.

**coverage:** ✓ covered

### 2. composition pattern (evolvable.architecture)

**requirement:** must show how BrainAtom is composed, not inherited

**blueprint filediffs (line 50):**
```
├─ [~] BrainArch1Repl.ts           # atom: BrainArch1Atom → BrainAtom
```

**extant BrainArch1Repl.ts (line 26):**
```ts
atom: BrainArch1Atom;
```

**analysis:** blueprint explicitly shows `atom:` property type change. BrainArch1Repl composes BrainAtom as a property - not via inheritance.

**coverage:** ✓ covered

### 3. type definitions (evolvable.domain.objects, pitofsuccess.typedefs)

**requirement:** must specify concrete types for new interfaces

**blueprint extension contract (lines 29-40):**
```ts
// input extension
ask<TOutput>(input: {
  prompt: string;
  schema: { output: z.Schema<TOutput> };
  tools?: ToolDefinition[];        // NEW: tool definitions
  on?: { episode: BrainEpisode };
  plugs?: BrainAtomPlugs;
  role: { briefs?: Artifact[] };
}) => Promise<BrainOutput<TOutput, 'atom'> & {
  toolInvocations?: ToolInvocation[];  // NEW: tool calls in response
}>
```

**analysis:** all types explicitly specified:
- ToolDefinition[] - tool interface
- ToolInvocation[] - tool call output
- BrainEpisode - episode continuation
- BrainOutput<TOutput, 'atom'> - generic output

**coverage:** ✓ covered

### 4. operation verbs (evolvable.domain.operations)

**requirement:** must use get/set/gen verbs

**blueprint operations:**
- `generateBrainArch1LlmResponse` - uses `generate` (LLM inference)
- `genBrainRepl` - uses `gen` (factory)

**analysis:** both operations follow verb conventions. no new operations introduced that violate patterns.

**coverage:** ✓ covered

### 5. error handle (pitofsuccess.errors)

**requirement:** must address error propagation

**search for try/catch in llm folder:**
```
grep: try.*catch|catch.*\(
path: src/domain.operations/arch1/llm/*.ts
found: No matches
```

**extant code has no try/catch** - errors propagate naturally.

**blueprint (line 13):**
```
**don't optimize what shouldn't exist yet.** no workarounds.
```

**analysis:** extant generateBrainArch1LlmResponse.ts has no try/catch. blueprint proposes same pattern - errors from atom.ask() propagate without hide. fail-fast maintained.

**coverage:** ✓ covered (verified via grep)

### 6. test updates (code.test)

**requirement:** must specify test changes

**search for extant test files:**
```
glob: src/**/*.test.ts
found 16 files:
  - generateBrainArch1LlmResponse.test.ts
  - iterateBrainArch1Loop.test.ts
  - invokeBrainArch1.test.ts
  - invokeBrainArch1.integration.test.ts
  - executeBrainArch1ToolCall.test.ts
  - (11 other test files)
```

**extant generateBrainArch1LlmResponse.test.ts (lines 4, 33-40):**
```ts
import type { BrainArch1Atom } from '@src/domain.objects/BrainArch1/BrainArch1Atom';

const createMockAtom = (
  generateFn: jest.Mock = jest.fn(),
): BrainArch1Atom => ({
  platform: 'test',
  model: 'test-atom',
  description: 'mock atom for testing',
  generate: generateFn,
});
```

**blueprint test section (lines 97-107):**
```
| generateBrainArch1LlmResponse.test.ts | mock BrainAtom instead of BrainArch1Atom |
| iterateBrainArch1Loop.test.ts | update atom mock |
| invokeBrainArch1.test.ts | update atom mock |
| invokeBrainArch1.integration.test.ts | use real BrainAtom |
```

**analysis:** blueprint identifies exactly which tests import BrainArch1Atom and need mock updates. verified extant test uses createMockAtom pattern - blueprint correctly identifies what to change.

**coverage:** ✓ covered (verified via glob + read)

### 7. response transformation (evolvable.procedures)

**requirement:** must show how response shapes are transformed

**blueprint codepaths (lines 88-91):**
```
└─ [~] transform response:
       ├─ output → message content
       ├─ toolInvocations → toolCalls
       └─ metrics → tokenUsage
```

**analysis:** blueprint explicitly maps rhachet output fields to bhrain fields. clear transformation contract.

**coverage:** ✓ covered

### 8. idempotency (pitofsuccess.procedures)

**requirement:** must address idempotent operations

**analysis:** generateBrainArch1LlmResponse is a stateless operation - it calls atom.ask() and returns the result. no state mutation. idempotency is inherent: same inputs produce same LLM call.

**coverage:** ✓ covered (stateless operation)

## gaps found

### 1. mock factory pattern

**question:** should blueprint specify genMockBrainAtom helper?

**blueprint (lines 67-68):**
```
└─ [~] .test/
   └─ [~] genMockBrainArch1Context.ts # update mock to use BrainAtom
```

**analysis:** blueprint mentions update to the mock file but doesn't detail the mock factory. however, this is implementation detail - the test section covers what needs to change.

**verdict:** acceptable - test updates section covers scope

### 2. episode continuation detail

**question:** should blueprint specify how episode continuation handles tool flow?

**blueprint blocked by (lines 120-131):**
```
3. **BrainExchange extension** - current BrainExchange has simple `input: string` and `output: string`. tool flow has more structure:
   - tool call (name, args) in output
   - tool result in subsequent input
   - multi-tool calls in single turn

   BrainExchange may need extension to represent tool flow
```

**analysis:** blueprint explicitly identifies this as a blocker for rhachet. correct - bhrain cannot specify what rhachet hasn't designed yet.

**verdict:** ✓ correctly deferred to rhachet

## coverage summary

| standard category | covered? | evidence |
|-------------------|----------|----------|
| contracts | ✓ | codepaths show input.* pattern |
| architecture | ✓ | filediffs show composition |
| domain objects | ✓ | extension contract specifies types |
| operations | ✓ | operations use correct verbs |
| procedures | ✓ | codepaths show transformation |
| errors | ✓ | fail-fast, no workarounds |
| types | ✓ | explicit TypeScript types |
| tests | ✓ | unit + integration tests listed |

## conclusion

blueprint covers all relevant mechanic standards.

**verified via actual searches:**

| standard | search | result |
|----------|--------|--------|
| contracts | grep `input: \{` in domain.operations | 10+ matches confirm pattern |
| composition | grep `atom:` in BrainArch1Repl.ts | line 26 shows composition |
| errors | grep `try.*catch` in arch1/llm | 0 matches - fail-fast |
| comments | grep `.what = \|.why = ` | 10+ matches confirm pattern |
| tests | glob `src/**/*.test.ts` | 16 test files found |

**blueprint coverage verified:**

1. contract pattern: codepaths show `input.atom`, `input.messages` (lines 80-91)
2. composition: filediffs show `atom:` property type change (line 50)
3. types: extension contract specifies ToolDefinition[], ToolInvocation[] (lines 29-40)
4. tests: 4 specific test files listed with required changes (lines 97-107)
5. errors: "no workarounds" follows fail-fast (line 13)
6. transformation: field-by-field map of rhachet → bhrain (lines 88-91)

no gaps found. all standards covered or correctly deferred to rhachet.

