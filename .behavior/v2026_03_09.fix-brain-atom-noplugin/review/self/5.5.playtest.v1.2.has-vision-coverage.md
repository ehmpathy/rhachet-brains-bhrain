# self-review: has-vision-coverage

## the question

does the playtest cover all behaviors? is every behavior in 0.wish.md verified? is every behavior in 1.vision.md verified? are any requirements left untested?

## analysis

### behaviors from 0.wish.md

**wish:**
> update the BrainArch1 to operate against rhachet's BrainAtoms directly
> no more custom atom plugins

| behavior | playtest step | covered? |
|----------|---------------|----------|
| operate against rhachet's BrainAtom | step 6: verify atom.ask() used | ✅ |
| no more custom atom plugins | step 1: verify plugins deleted | ✅ |

### behaviors from 1.vision.md

**vision key points:**

1. **rhachet's BrainAtom IS the generalization** — bhrain uses rhachet's contract
2. **no custom atom interface** — atom plugins directory deleted
3. **atoms come from rhachet packages** — imports from rhachet-brains-anthropic/openai
4. **bhrain registers as repl provider** — no atom factory exports

| behavior | playtest step | covered? |
|----------|---------------|----------|
| uses rhachet's BrainAtom contract | step 6: verify atom.ask() | ✅ |
| atom plugins deleted | step 1: directory gone | ✅ |
| atoms from rhachet packages | step 2: grep imports | ✅ |
| no bhrain atom imports | step 3: grep for old imports | ✅ |
| agentic loop works | step 5: integration tests | ✅ |
| tool use works | step 5: tool use test case | ✅ |
| multi-turn works | step 5: multi-turn test case | ✅ |

### requirements left untested?

reviewed wish and vision line by line. all stated behaviors have matched playtest steps.

**coverage matrix:**

| requirement source | total behaviors | covered | gaps |
|--------------------|-----------------|---------|------|
| 0.wish.md | 2 | 2 | 0 |
| 1.vision.md | 7 | 7 | 0 |

## deeper reflection

### line-by-line verification from 0.wish.md

the wish file contains 3 behavioral statements (lines 3, 5, 7-9):

| line | text | interpretation | playtest step |
|------|------|----------------|---------------|
| 3 | "update the BrainArch1 to operate against rhachet's BrainAtom's directly" | BrainArch1 uses BrainAtom.ask() | step 6: grep for atom.ask |
| 5 | "no more custom atom plugins" | plugins directory deleted | step 1: ls shows directory gone |
| 7-9 | "Rhachet's BrainAtoms were born from... now its time to upgrade" | historical context, not testable behavior | n/a (narrative) |

**verification**: lines 3 and 5 are testable behaviors. line 7-9 is narrative context. the playtest covers both testable behaviors.

### line-by-line verification from 1.vision.md

the vision file is longer (156 lines). extracted testable behaviors:

| section | behavior | evidence | playtest step |
|---------|----------|----------|---------------|
| line 5 | "rhachet's BrainAtom IS the generalization" | concept, not testable | n/a |
| line 19 | "no manual imports. no custom atom interface." | no bhrain atom imports | step 3 |
| lines 29-31 | "before: plugins/atoms/ with anthropic.ts, openai.ts" | directory deleted | step 1 |
| line 40 | "after: no atom plugins" | directory deleted | step 1 |
| lines 50, 54 | "before: import from rhachet-brains-bhrain/plugins/atoms" | old pattern gone | step 3 |
| line 69 | "zero custom atoms: bhrain uses rhachet's BrainAtom directly" | atom.ask() used | step 6 |
| line 75 | "BrainArch1 internals must use BrainAtom.ask() instead of generate()" | atom.ask() used | step 6 |
| line 123 | "one interface, many providers, zero custom plugins" | integration tests pass with both providers | step 5 |

### why the coverage is complete

1. **structural coverage**: the playtest follows the behavior -> test structure from vision "before/after" section
2. **functional coverage**: integration tests (step 5) exercise the actual runtime behavior
3. **negative tests**: step 3 verifies absence of old patterns (no bhrain atom imports)

### what the playtest does NOT cover (and why that's ok)

| not covered | reason |
|-------------|--------|
| rhachet discovery via genContextBrain | out of scope — rhachet owns that, not bhrain |
| composite slug parse | out of scope — rhachet owns that |
| tool declaration utilities | deferred to rhachet handoff — not part of this refactor |

these are rhachet responsibilities. bhrain only verifies its half of the contract.

## review findings

### non-issues (holds)

#### 1. wish behaviors covered — holds

**why it holds**: the wish has two behaviors: (1) use rhachet's BrainAtom, (2) no custom plugins. both are verified by playtest steps 1 and 6.

#### 2. vision behaviors covered — holds

**why it holds**: the vision describes 7 observable behaviors. each maps to a playtest step:
- contract conformance → step 6
- plugin deletion → step 1
- import source → steps 2, 3
- agentic loop → step 5 (simple, tool use, multi-turn)

#### 3. no gaps found — holds

**why it holds**: walked through wish and vision line by line. every stated behavior has a matched verification step in the playtest.

### found issues

none found. vision coverage is complete.

### lessons learned

#### 1. line-by-line verification catches gaps

the wish has 3 statements but only 2 are testable. line 7-9 is narrative context. without line-by-line review, might have worried about "missing" coverage for the historical explanation.

#### 2. vision "before/after" sections are test blueprints

the vision's before/after section (lines 25-43) directly maps to playtest steps:
- before has `plugins/atoms/anthropic.ts` → step 1 verifies deletion
- after says "no atom plugins" → step 1 confirms

this pattern: vision.before is what to delete, vision.after is what to verify.

#### 3. negative tests are coverage

step 3 (grep for old imports) is a negative test — it verifies absence. this is just as important as positive verification. coverage includes "thing no longer exists" behaviors.

## conclusion

✅ all wish behaviors covered (2/2)
✅ all vision behaviors covered (7/7)
✅ no requirements left untested
✅ verified via line-by-line review of source documents
