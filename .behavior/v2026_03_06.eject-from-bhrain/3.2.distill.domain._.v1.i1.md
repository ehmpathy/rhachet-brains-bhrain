# distill: domain

> domain objects and operations for BrainArch1

---

## domain.objects

### entities

| object | purpose | unique key |
|--------|---------|------------|
| BrainArch1Actor | configured actor with atom, role, tools, constraints | n/a (configuration) |
| BrainArch1LoopResult | result of brain invocation | n/a (transient) |
| BrainArch1SessionMessage | message in conversation history | n/a (sequence) |

### literals (immutable value objects)

| object | purpose |
|--------|---------|
| BrainArch1Context | execution context for brain operations |
| BrainArch1PermissionDecision | allow/deny verdict for tool call |
| BrainArch1ToolCall | tool invocation request |
| BrainArch1ToolResult | tool execution result |
| BrainArch1ToolDefinition | tool schema declaration |
| BrainArch1MemoryTokenUsage | token count tracker |
| BrainArch1Iteration | single loop iteration details |

### guards/plugins

| object | purpose |
|--------|---------|
| BrainArch1PermissionGuard | plugin interface for tool authorization |
| BrainArch1MemoryManager | plugin interface for context optimization |

---

## domain.operations

### core operations

| operation | verb | purpose |
|-----------|------|---------|
| invokeBrainArch1 | invoke | entry point: run brain loop with actor and input |
| runBrainArch1Loop | run | execute the agentic loop |
| mergeBrainArch1Toolboxes | merge | combine toolboxes into unified tool set |

### atom operations (per provider)

| operation | verb | purpose |
|-----------|------|---------|
| genAtomAnthropic | gen | create anthropic atom |
| genAtomOpenai | gen | create openai atom |
| genAtomQwen | gen | create qwen atom |

### toolbox operations

| operation | verb | purpose |
|-----------|------|---------|
| toolboxFiles | n/a | file read/write tools |
| toolboxBash | n/a | bash execution tools |
| toolboxWeb | n/a | web fetch tools |

---

## relationships

```
BrainArch1Actor
├── atom (genAtom*)
├── role.systemPrompt
├── toolboxes[]
│   └── BrainArch1ToolDefinition[]
├── constraints
├── memory (BrainArch1MemoryManager | null)
└── permission (BrainArch1PermissionGuard | null)

invokeBrainArch1
├── input: { actor, userInput, conversationHistory? }
├── calls: runBrainArch1Loop
└── returns: BrainArch1LoopResult
    ├── finalResponse
    ├── terminationReason
    ├── messages: BrainArch1SessionMessage[]
    ├── iterations: BrainArch1Iteration[]
    └── totalTokenUsage
```

---

## composition for wish

the ejection brings these domain objects and operations from roles repo into brains repo. this enables:

1. `npm install rhachet-brains-bhrain`
2. `import { invokeBrainArch1, BrainArch1Actor } from 'rhachet-brains-bhrain'`
3. configure actor → invoke brain → receive result

---

🐢 domain distilled: 13 objects, 7+ operations, clear hierarchy

