# roadmap: eject BrainArch1 into rhachet-brains-bhrain

> checklist-style execution plan with behavioral acceptance criteria

---

## phase.0: prepare

**prereads**: none

### checklist

- [ ] verify worktree exists at `/home/vlad/git/ehmpathy/_worktrees/rhachet-roles-bhrain.vlad.eject-brain-arch1`
- [ ] verify source files exist: `$FROM/src/domain.objects/BrainArch1`
- [ ] verify destination repo exists: `~/git/ehmpathy/rhachet-brains-bhrain`

### acceptance criteria

```
given the ejection is about to begin
  when we check prerequisites
    then /home/vlad/git/ehmpathy/_worktrees/rhachet-roles-bhrain.vlad.eject-brain-arch1 exists
    then src/domain.objects/BrainArch1 exists in worktree
    then ~/git/ehmpathy/rhachet-brains-bhrain exists
```

### verification

```bash
ls /home/vlad/git/ehmpathy/_worktrees/rhachet-roles-bhrain.vlad.eject-brain-arch1/src/domain.objects/BrainArch1
ls ~/git/ehmpathy/rhachet-brains-bhrain
```

---

## phase.1: rename anthropic → bhrain

**prereads**: none

**depends on**: phase.0

### checklist

- [ ] update package.json name: `rhachet-brains-anthropic` → `rhachet-brains-bhrain`
- [ ] update package.json description: `rhachet brain.repl supplier for bhrain (BrainArch1)`

### acceptance criteria

```
given the package identity needs to change
  when we update package.json
    then name = 'rhachet-brains-bhrain'
    then description references bhrain
```

### verification

```bash
grep '"name"' package.json | grep 'rhachet-brains-bhrain'
grep '"description"' package.json | grep 'bhrain'
```

---

## phase.2: add deps + prune src/

**prereads**: none

**depends on**: phase.1

### checklist

- [ ] add runtime deps: `as-procedure`, `fast-glob`
- [ ] add peerDependencies: `rhachet-brains-anthropic`, `rhachet-brains-openai`
- [ ] add devDependencies (for tests): `rhachet-brains-anthropic`, `rhachet-brains-openai`
- [ ] remove: `src/domain.operations/atoms/`
- [ ] remove: `src/domain.operations/repls/`
- [ ] remove: `src/domain.operations/hooks/`
- [ ] remove: `src/infra/`
- [ ] remove: `src/.test/`
- [ ] remove: `src/access/sdk/`
- [ ] remove: `src/access/sdks/`
- [ ] keep: `src/contract/sdk/` (for update in phase.4)

### acceptance criteria

```
given anthropic-specific code needs removal
  when we prune src/
    then src/domain.operations/atoms/ does not exist
    then src/domain.operations/repls/ does not exist
    then src/domain.operations/hooks/ does not exist
    then src/infra/ does not exist
    then src/contract/sdk/ still exists (for update)
  when we check package.json
    then peerDependencies includes rhachet-brains-anthropic
    then peerDependencies includes rhachet-brains-openai
    then devDependencies includes rhachet-brains-anthropic
    then devDependencies includes rhachet-brains-openai
```

### verification

```bash
# verify removals
! ls src/domain.operations/atoms/ 2>/dev/null && echo "✓ atoms removed"
! ls src/domain.operations/repls/ 2>/dev/null && echo "✓ repls removed"
! ls src/domain.operations/hooks/ 2>/dev/null && echo "✓ hooks removed"
ls src/contract/sdk/ && echo "✓ contract/sdk retained"

# verify deps
grep -A5 '"peerDependencies"' package.json
grep -A5 '"devDependencies"' package.json
```

---

## phase.3: run ejection

**prereads**:
- `.behavior/v2026_03_06.eject-from-bhrain/refs/HANDOFF.md`

**depends on**: phase.2

### checklist

- [ ] run ejection command from HANDOFF.md
- [ ] verify 68 src files received (67 moved, 1 copied)
- [ ] verify `src/domain.objects/BrainArch1/` exists with 19 files
- [ ] verify `src/domain.operations/arch1/` exists with 41 files
- [ ] verify `src/access/sdk/` exists
- [ ] verify `src/access/sdks/` exists
- [ ] verify `src/.test/` exists with test utilities
- [ ] eject architect briefs (28 files) to `.agent/repo=.this/role=architect/briefs/`

### acceptance criteria

```
given the ejection command runs
  when it completes
    then src/domain.objects/BrainArch1/ contains 19 files
    then src/domain.operations/arch1/ contains ~41 files
    then src/access/sdk/ exists
    then src/access/sdks/ exists
    then src/.test/genMockBrainArch1Context.ts exists
    then src/.test/genContextLogTrail.ts exists
    then .agent/repo=.this/role=architect/briefs/ contains 28 files
```

### verification

```bash
ls src/domain.objects/BrainArch1/ | wc -l
ls src/domain.operations/arch1/ -R | grep -c ".ts"
ls src/access/sdk/
ls src/access/sdks/
ls src/.test/
ls .agent/repo=.this/role=architect/briefs/ | wc -l
```

---

## phase.4: post-ejection transforms

**prereads**:
- `.behavior/v2026_03_06.eject-from-bhrain/3.3.blueprint.v1.i1.md` (transformations section)
- `.behavior/v2026_03_06.eject-from-bhrain/3.1.research.patterns._.code.prod.v1.i1.md` (if declared)

**depends on**: phase.3

### checklist

- [ ] update imports: `@src/domain.operations/brain.replic.arch1` → `@src/domain.operations/arch1`
- [ ] transform: `BrainArch1Actor` → `BrainArch1Repl`
- [ ] transform: `BrainArch1Atom` → `BrainAtom` (from rhachet)
- [ ] create: `src/domain.operations/brain.repl/genBrainRepl.ts`
- [ ] implement composite slug pattern: `bhrain/arch1@$atom`
- [ ] update: `src/contract/sdk/index.ts` to export genBrainRepl
- [ ] update: `src/index.ts` to re-export sdk
- [ ] run: `pnpm install`

### acceptance criteria

```
given the ejected code needs transformation
  when transforms are applied
    then no imports reference 'brain.replic.arch1'
    then no references to 'BrainArch1Actor' exist
    then genBrainRepl({ slug: 'bhrain/arch1@claude/sonnet' }) compiles
    then src/contract/sdk/index.ts exports genBrainRepl
```

### verification

```bash
# no old imports
! grep -r 'brain.replic.arch1' src/ && echo "✓ imports updated"

# no BrainArch1Actor
! grep -r 'BrainArch1Actor' src/ && echo "✓ Actor → Repl renamed"

# genBrainRepl exists
ls src/domain.operations/brain.repl/genBrainRepl.ts

# exports correct
grep 'genBrainRepl' src/contract/sdk/index.ts
grep 'genBrainRepl' src/index.ts
```

---

## phase.5: verify build

**prereads**: none

**depends on**: phase.4

### checklist

- [ ] `npm run build` succeeds
- [ ] `npm run test:types` passes

### acceptance criteria

```
given the codebase is transformed
  when we run build
    then compilation succeeds with no errors
  when we run test:types
    then type checks pass
```

### verification

```bash
npm run build
npm run test:types
```

---

## phase.6: verify tests

**prereads**:
- `.behavior/v2026_03_06.eject-from-bhrain/3.1.research.patterns._.code.test.v1.i1.md` (if declared)

**depends on**: phase.5

### checklist

- [ ] `npm run test:unit` passes
- [ ] `npm run test:integration` passes

### acceptance criteria

```
given the build passes
  when we run unit tests
    then all unit tests pass
  when we run integration tests
    then all integration tests pass
```

### verification

```bash
npm run test:unit
npm run test:integration
```

---

## phase.7: verify acceptance

**prereads**:
- `.behavior/v2026_03_06.eject-from-bhrain/2.1.criteria.blackbox.md`

**depends on**: phase.6

### checklist

- [ ] `npm run test:acceptance:locally` passes
- [ ] usecase.1: install and import works
- [ ] usecase.2: composite slug `bhrain/arch1@claude/sonnet` works
- [ ] usecase.4: ask (readonly) vs act (read+write) modes work
- [ ] usecase.5: `bhrain/arch1` alone throws BadRequestError

### acceptance criteria

```
given all tests pass
  when we verify blackbox criteria
    then import { genBrainRepl } from 'rhachet-brains-bhrain' succeeds
    then genContextBrain({ choice: 'bhrain/arch1@claude/sonnet' }) returns BrainRepl
    then brain.ask() returns BrainOutput with readonly tools
    then brain.act() returns BrainOutput with read+write tools
    then bhrain/arch1 alone throws BadRequestError
```

### verification

```bash
npm run test:acceptance:locally
```

---

## summary

| phase | description | depends on |
|-------|-------------|------------|
| 0 | prepare (verify prerequisites) | - |
| 1 | rename anthropic → bhrain | 0 |
| 2 | add deps + prune src/ | 1 |
| 3 | run ejection (68 src files + 28 architect briefs) | 2 |
| 4 | post-ejection transforms | 3 |
| 5 | verify build | 4 |
| 6 | verify tests | 5 |
| 7 | verify acceptance | 6 |

---

🐢 roadmap complete — 8 phases, sequential execution, behavioral verification at each step

