# self-review: has-questioned-assumptions

> surfacing hidden assumptions taken as truth

---

## hidden assumptions identified

### 1. "BrainArch1 is ready for extraction"

**assumption**: BrainArch1 is mature, stable, and complete enough to extract.

| question | reflection |
|----------|------------|
| evidence? | the handoff exists, which suggests someone thought it was ready |
| what if opposite true? | if BrainArch1 is still evolving, ejecting now creates version drift between repos |
| did wisher say this? | no — the wisher said "due your due diligence" which implies verification needed |
| exceptions? | BrainArch1 might have known issues or planned changes |

**verdict**: this assumption is UNVERIFIED. should check:
- is there active development on BrainArch1 in roles repo?
- are there open issues or planned changes?
- what version is considered "stable"?

---

### 2. "BrainArch1 has minimal coupling to roles repo"

**assumption**: the 100 files can be moved without breaking due to dependencies on role-specific code.

| question | reflection |
|----------|------------|
| evidence? | handoff provides file list but doesn't analyze dependencies |
| what if opposite true? | imports could reference role-specific utilities, domain objects, or configs |
| did wisher say this? | no — wisher said "ensure a smooth ejection" which implies we must verify |
| exceptions? | could have imports like `@src/domain.roles/bhrain/...` that won't exist post-ejection |

**investigation performed**:

```bash
grep -r "import.*from" .../brain.replic.arch1/core/ | head -20
```

**findings**: all imports reference internal paths:
- `@src/domain.objects/BrainArch1/*` — being ejected
- `@src/domain.operations/brain.replic.arch1/*` — being ejected
- `@src/.test/*` — being ejected
- `@src/access/sdks/*` — being ejected
- external packages (test-fns, helpful-errors) — already in deps

**no role-specific imports found**. BrainArch1 is self-contained.

**verdict**: assumption VERIFIED as true — minimal coupling confirmed via grep

---

### 3. "the api contract i imagined is accurate"

**assumption**: `genBrainArch1({ slug: '...' })` returns a brain with `.ask()` method accepting `prompt`, `sandbox`, and `on.episode`.

| question | reflection |
|----------|------------|
| evidence? | i inferred from brain.repl patterns, not from actual source |
| what if opposite true? | api might have different method names, different input shapes |
| did wisher say this? | no — i extrapolated based on naming conventions |
| exceptions? | BrainArch1 might use completely different abstractions |

**verdict**: this assumption is GUESSED. i should verify the actual api before finalizing vision.

---

### 4. "episode continuation works for BrainArch1"

**assumption**: the vision shows multi-turn conversation with `on: { episode }`.

| question | reflection |
|----------|------------|
| evidence? | the brief `define.episode-continuation-limits.md` says repls DON'T support continuation |
| what if opposite true? | continuation doesn't work — the vision example would fail |
| did wisher say this? | no — i assumed based on generic repl behavior |
| exceptions? | BrainArch1 might have worked around the limitation |

**verdict**: this assumption CONTRADICTS known documentation. repl continuation is NOT supported per the brief. the vision example is likely WRONG.

**action needed**: remove or qualify the continuation example in vision.

---

### 5. "developers want to use brains without roles"

**assumption**: there's a usecase for importing BrainArch1 directly without the full bhrain role machinery.

| question | reflection |
|----------|------------|
| evidence? | architectural separation patterns suggest this is a good practice |
| what if opposite true? | maybe no one uses brains without roles; separation is premature |
| did wisher say this? | no — the wisher said to eject, not why users would want it |
| exceptions? | roles might always be needed to configure the brain properly |

**verdict**: this assumption is PLAUSIBLE but UNVERIFIED. makes architectural sense, but real-world demand is assumed.

---

### 6. "the worktree directory still exists"

**assumption**: the handoff references a specific worktree path that exists.

```
FROM="/home/vlad/git/ehmpathy/_worktrees/rhachet-roles-bhrain.vlad.eject-brain-arch1"
```

| question | reflection |
|----------|------------|
| evidence? | the path is hardcoded in the handoff |
| what if opposite true? | if worktree was cleaned up, ejection command fails |
| did wisher say this? | the handoff assumes it exists |
| exceptions? | worktrees are temporary; could have been removed |

**verdict**: this assumption must be VERIFIED before execution.

---

### 7. "removing anthropic code doesn't break consumers"

**assumption**: we can prune current src/ without breaking anyone who depends on rhachet-brains-anthropic.

| question | reflection |
|----------|------------|
| evidence? | wait — this repo is a CLONE, not the original |
| what if opposite true? | if this IS the original, consumers would break |
| did wisher say this? | handoff says "this repo was cloned from rhachet-brains-anthropic" |
| exceptions? | the repo might be a fork with same npm name, causing confusion |

**verdict**: this assumption is CORRECT per handoff. this is a clone becoming a new package, not modifying the original.

---

### 8. "hooks adapter functionality is preserved"

**assumption**: hooks adapter for claude code will still work after ejection.

| question | reflection |
|----------|------------|
| evidence? | handoff mentions access/sdk/ and access/sdks/ but not hooks/ |
| what if opposite true? | hooks functionality might be lost, breaking claude code integration |
| did wisher say this? | no — this is my concern from earlier review |
| exceptions? | BrainArch1 might include hooks in a different location |

**verdict**: this assumption is UNVERIFIED. need to check:
- does BrainArch1 include hooks?
- if not, should we preserve current hooks alongside ejected code?

---

## summary of assumptions by status

| assumption | status | risk |
|------------|--------|------|
| BrainArch1 is ready | unverified | medium — could eject immature code |
| minimal coupling | unverified | high — broken imports would fail build |
| api contract accurate | guessed | medium — vision misrepresents ux |
| episode continuation works | contradicts docs | high — vision shows broken example |
| developers want brains without roles | plausible | low — architectural preference |
| worktree exists | must verify | high — ejection command would fail |
| not breaking consumers | correct | low — this is a clone |
| hooks preserved | unverified | medium — could lose functionality |

---

## critical finding

**episode continuation is documented as NOT SUPPORTED for repls.**

the vision shows:

```ts
const step2 = await brain.ask({
  on: { episode: step1.episode }
});
```

this is likely WRONG for BrainArch1 if it's a repl implementation.

**action**: update vision to either:
1. remove continuation example, or
2. note the limitation explicitly

---

## additional assumptions found on second pass

### 9. "leverages claude agent sdk under the hood"

**assumption**: line 20 claimed BrainArch1 uses claude agent sdk.

| question | reflection |
|----------|------------|
| evidence? | i assumed based on the repo having repl code |
| what if opposite true? | BrainArch1 is provider-agnostic with pluggable atoms |
| did wisher say this? | no — the readme says "provider-agnostic brain.repl" |

**verdict**: WRONG. BrainArch1 does NOT use claude agent sdk. it has its own loop implementation.

**fixed**: updated vision to say "provider-agnostic — pluggable atoms for anthropic, openai, qwen".

---

### 10. "sandboxed execution (read-only or workspace-write)"

**assumption**: vision mentioned sandbox modes.

| question | reflection |
|----------|------------|
| evidence? | i assumed based on other brain.repl patterns |
| what if opposite true? | BrainArch1 uses permission guards, not sandbox modes |
| did wisher say this? | no — i invented this terminology |

**verdict**: WRONG terminology. BrainArch1 uses `BrainArch1PermissionGuard` not sandbox modes.

**fixed**: updated vision to say "supports permission guards for controlled tool execution".

---

### 11. "invalid slug" edgecase

**assumption**: line 201 mentioned type-safe slugs.

| question | reflection |
|----------|------------|
| evidence? | i assumed from rhachet brain patterns |
| what if opposite true? | BrainArch1 takes an actor, not a slug |
| did wisher say this? | no — i extrapolated incorrectly |

**verdict**: WRONG. BrainArch1 api is `invokeBrainArch1({ actor, userInput }, context)` — no slugs.

**fixed**: replaced edgecase with actual BrainArch1 patterns (allowAll guard, token limit, max iterations, empty input).

---

## actions taken

### ✅ verified worktree exists

```
ls -la /home/vlad/git/ehmpathy/_worktrees/rhachet-roles-bhrain.vlad.eject-brain-arch1
```

result: worktree is present and ready.

### ✅ checked actual BrainArch1 api

read the source:
- `src/domain.operations/brain.replic.arch1/core/invokeBrainArch1.ts`
- `src/domain.objects/BrainArch1/BrainArch1Actor.ts`

**found**: the actual api is `invokeBrainArch1({ actor, userInput, conversationHistory? }, context)`

NOT `genBrainArch1({ slug }).ask({ prompt })` as i imagined.

**fixed**: updated vision with correct api examples.

### ✅ confirmed continuation support

**found**: continuation IS supported via `conversationHistory` parameter.

this is different from the repl-via-sdk limitation — BrainArch1 manages its own message history.

**fixed**: updated vision examples to show correct continuation pattern.

### ✅ hooks coverage VERIFIED

**investigation**: checked roles repo structure:

```
src/domain.operations/
├── brain.replic.arch1/   ← being ejected
└── hooks/                ← NOT being ejected (separate directory)
```

**findings**:
- hooks in roles repo is a SEPARATE directory from brain.replic.arch1
- BrainArch1 ejection does NOT include hooks
- this brains repo already has its own hooks implementation (`src/domain.operations/hooks/`)

**verdict**: hooks coverage is NOT a concern. we keep our current hooks implementation alongside the ejected BrainArch1 code. no action needed.

---

## summary

**first pass:**
- 8 assumptions identified
- 3 critical issues found
- 2 fixed, 1 deferred

**second pass (slow, fresh eyes):**
- 3 more assumptions found
- all 3 were WRONG in the vision
- all 3 now fixed

**total:**
- 11 assumptions questioned
- 5 were wrong or misleading → 5 now fixed in vision
- 2 key assumptions verified via investigation:
  - minimal coupling: confirmed via grep (no role-specific imports)
  - hooks coverage: confirmed separate from BrainArch1 (keep current hooks)

the vision now accurately reflects the BrainArch1 api, terminology, and edgecases.

---

🐢 thorough investigation, issues fixed, assumptions verified

