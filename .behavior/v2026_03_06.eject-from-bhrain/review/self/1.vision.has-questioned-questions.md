# self-review: has-questioned-questions

> triage of open questions from the vision

---

## open questions from vision

### 1. dependencies: does BrainArch1 require any packages not yet in this repo's deps?

**can this be answered via code?** yes — compare package.json files

**investigation performed:**

compared `rhachet-roles-bhrain/package.json` with `rhachet-brains-bhrain/package.json`:

| package | in roles repo | in brains repo | action needed |
|---------|---------------|----------------|---------------|
| `as-procedure` | 1.1.7 | ❌ absent | add |
| `fast-glob` | 3.3.3 | ❌ absent | add |
| `openai` | 5.8.2 | ❌ absent | add (BrainArch1 is provider-agnostic) |
| `serde-fns` | 1.2.0 | ❌ absent | check if used |
| `simple-in-memory-cache` | 0.4.0 | ❌ absent | check if used |
| `with-simple-cache` | 0.14.2 | ❌ absent | check if used |
| `wrapper-fns` | 1.1.0 | ❌ absent | check if used |
| `@anthropic-ai/sdk` | 0.51.0 | 0.71.2 | already present (newer) |
| `domain-objects` | 0.31.9 | 0.31.9 | already present |
| `helpful-errors` | 1.5.3 | 1.5.3 | already present |
| `type-fns` | 1.21.0 | 1.21.0 | already present |
| `zod` | 4.3.4 | 4.3.4 | already present |
| `rhachet-artifact` | 1.0.0 | 1.0.1 | already present (newer) |
| `rhachet-artifact-git` | 1.1.0 | 1.1.5 | already present (newer) |

**verdict**: ANSWERED — need to add at minimum: `as-procedure`, `fast-glob`, `openai`. other packages (`serde-fns`, `simple-in-memory-cache`, `with-simple-cache`, `wrapper-fns`) should be verified via imports in ejected files.

**action**: add to criteria — verify and add absent dependencies post-ejection.

---

### 2. imports: are there circular dependencies that will break when split?

**can this be answered via code?** yes — grep for imports

**investigation performed:**

ran grep to identify all `@src` imports in BrainArch1:
```bash
grep -r "from '@src" brain.replic.arch1/ | head -30
```

**actual import paths found**:
```
@src/domain.objects/BrainArch1/*        → being ejected ✓
@src/domain.operations/brain.replic.arch1/*  → being ejected ✓
@src/.test/*                             → being ejected ✓
@src/access/sdks/anthropic/*             → being ejected ✓
```

**findings**:
- **zero imports** reference role-specific code (e.g., `@src/domain.roles/bhrain/...`)
- all imports point to directories explicitly listed in the handoff ejection inventory
- no circular dependencies — the structure is fully self-contained

**verdict**: ANSWERED — no circular dependencies. BrainArch1 is self-contained, all imports point to ejected code.

---

### 3. tests: do all tests pass standalone, or do some require role-repo fixtures?

**can this be answered via code?** partially — can inspect test files for fixture dependencies

**investigation planned**:
- post-ejection, run `npm run test:types` to verify compilation
- run `npm run test:unit` to verify unit tests pass
- run `npm run test:integration` to verify integration tests pass

**verdict**: DEFERRED to execution phase — tests must be run post-ejection to verify. this is standard verification, not a question that blocks.

**action**: add to criteria — all tests must pass post-ejection.

---

### 4. name: is `rhachet-brains-bhrain` the final package name?

**can this be answered via code?** no — this is a name decision

**observations**:
- repo name is `rhachet-brains-bhrain`
- package.json currently says `rhachet-brains-anthropic` (from the clone)
- handoff says to rename anthropic → bhrain

**wisher confirmed**: yes, `rhachet-brains-bhrain` is the final package name. rename contents from anthropic to bhrain.

**verdict**: ANSWERED — proceed with rename anthropic → bhrain

---

## summary

| question | status | action |
|----------|--------|--------|
| dependencies | ANSWERED | add absent deps: `as-procedure`, `fast-glob`, `openai`, verify others |
| circular imports | ANSWERED | none — BrainArch1 is self-contained |
| tests pass | DEFERRED | verify post-ejection (standard practice) |
| package name | ANSWERED | confirmed `rhachet-brains-bhrain`, rename anthropic → bhrain |

---

## recommendation

proceed to criteria phase with:
1. dependency verification as explicit criterion
2. test passage as verification criterion
3. rename anthropic → bhrain as explicit transformation step
4. post-ejection: consider rename BrainArch1Actor → BrainRepl terminology (if needed)

---

🐢 questions triaged, 3 answered (2 via investigation, 1 via wisher), 1 deferred to execution

