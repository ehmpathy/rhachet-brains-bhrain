# self-review: has-pruned-backcompat

> review for backwards compatibility that was not explicitly requested

---

## methodology

scanned blueprint 3.3.blueprint.v1.i1.md for:
- aliases (old name → new name kept for compat)
- deprecation notices
- "keep X for backwards compatibility" statements
- gradual migration paths
- dual export patterns

for each backwards-compat concern found, ask:
1. did the wisher explicitly say to maintain this compatibility?
2. is there evidence this backwards compat is needed?
3. or did we assume it "to be safe"?

---

## context

this is a **transformation**, not an upgrade:
- rhachet-brains-anthropic → rhachet-brains-bhrain
- anthropic atom code → BrainArch1 brain.repl code
- different package name, different purpose

the wisher explicitly said:
> "we need to rename the contents of this template shell repo from anthropic to bhrain"

this signals a clean break, not a backwards-compatible evolution.

---

## backwards-compat concerns reviewed

### 1. anthropic exports

**what blueprint does:**
- phase 2: removes all anthropic code (BrainAtom*, BrainRepl*, genBrainAtom.ts)
- no aliases or deprecation warnings
- complete removal

**backwards compat added?** NO

**evidence it's needed?** NO — wisher said to rename anthropic → bhrain

**assumed "to be safe"?** NO — clean break was requested

**verdict**: CORRECT — no backwards compat, clean transformation

---

### 2. import paths

**what blueprint does:**
- phase 4: sedreplace `@src/domain.operations/brain.replic.arch1` → `@src/domain.operations/arch1`
- no path aliases for old locations
- no gradual migration

**backwards compat added?** NO

**evidence it's needed?** NO — this is internal to the repo, not external API

**assumed "to be safe"?** NO

**verdict**: CORRECT — direct rename, no compat shim needed

---

### 3. package.json name

**what blueprint does:**
- phase 1: renames `rhachet-brains-anthropic` → `rhachet-brains-bhrain`
- no npm alias
- no deprecation notice

**backwards compat added?** NO

**evidence it's needed?** NO — new package, not upgrade of old

**assumed "to be safe"?** NO

**question**: should old package be deprecated?

**verdict**: OUT OF SCOPE — old package (rhachet-brains-anthropic) is separate repo, not this ejection

---

### 4. hooks adapter

**what blueprint does:**
- retains genBrainHooksAdapterForClaudeCode
- no changes to hooks API

**backwards compat added?** NO — hooks are retained as-is

**evidence it's needed?** N/A — no change means no compat concern

**verdict**: CORRECT — retain, no compat concern

---

## blueprint sections scanned

### contracts section (lines 7-32)
- exports only new BrainArch1 API
- NO re-exports of old anthropic types
- NO aliases like `export { BrainArch1Actor as AnthropicBrainActor }`

### treestruct: filediffs section (lines 36-84)
- `[-]` delete markers for old anthropic code
- `[←]` eject markers for new BrainArch1 code
- NO `[~]` update markers that add compat shims
- NO "keep old alongside new" patterns

### execution phases section (lines 129-190)
- phase.2: removes anthropic code entirely (`rm -rf`)
- phase.4: direct rename of import paths
- NO "add alias for old path" steps
- NO deprecation notices emitted

---

## backwards-compat found

| concern | backwards compat added? | evidence |
|---------|------------------------|----------|
| anthropic exports | NO (removed) | phase.2 uses `rm -rf` |
| import paths | NO (direct rename) | phase.4 uses sedreplace, no alias |
| package name | NO (new package) | phase.1 renames, no npm alias |
| hooks adapter | NO (retained as-is) | marked `[○] retain`, not compat |

---

## open questions

none — this is a transformation, not an upgrade

the wisher explicitly requested the rename, which implies clean break:
- no consumers of old anthropic exports to support
- no gradual migration path needed
- new package identity

---

## summary

- 0 backwards-compat concerns added
- transformation is clean break (as requested)
- no aliases, shims, or deprecation warnings

---

🐢 backwards-compat review complete — clean transformation, no compat shims added

