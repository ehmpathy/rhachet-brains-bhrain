# self-review: has-questioned-requirements

> questioning the vision requirements with fresh eyes

---

## requirements examined

### 1. eject BrainArch1 from roles repo to brains repo

| question | answer |
|----------|--------|
| who said this? | the wish from the human, based on handoff from roles-repo clone |
| why? | separation of concerns — brains belong in brains packages, roles in roles packages |
| evidence? | the handoff document provides clear rationale; BrainArch1 is a brain, not a role |
| what if we didn't? | BrainArch1 stays coupled with role machinery, harder to reuse independently |
| simpler way? | could re-export from roles, but that doesn't achieve true separation |

**verdict**: this requirement is sound. the separation aligns with rhachet's architecture pattern (`rhachet-brains-{supplier}`, `rhachet-roles-{name}`).

---

### 2. rename anthropic → bhrain

| question | answer |
|----------|--------|
| who said this? | the handoff document |
| why? | this repo is a clone being transformed into bhrain's brain package |
| evidence? | repo is named `rhachet-brains-bhrain`; package.json still says `rhachet-brains-anthropic` |
| what if we didn't? | naming confusion — package named anthropic but contains bhrain code |
| simpler way? | no, the rename is necessary for clarity |

**critical observation**: this is NOT modifying the original rhachet-brains-anthropic repo. this IS a clone being transformed. the original anthropic package remains intact.

**verdict**: requirement is sound. transformation makes sense for a cloned repo.

---

### 3. prune src/ (remove anthropic-specific code)

| question | answer |
|----------|--------|
| who said this? | the handoff |
| why? | BrainArch1 brings its own atoms; keeping both would cause confusion/conflicts |
| evidence? | handoff states "BrainArch1 includes its own atoms" |
| what if we didn't? | dual implementations, unclear which to use, potential conflicts |
| simpler way? | could merge, but defeats purpose of clean bhrain package |

**concern raised**: the current repo has `genBrainHooksAdapterForClaudeCode` — is this preserved in BrainArch1 or lost?

looking at handoff file inventory:
- `access/sdk/` — yes
- `access/sdks/` — yes
- but no explicit mention of hooks

**question to validate**: does BrainArch1 include hooks adapter, or should we preserve the current one?

**verdict**: requirement is sound IF hooks functionality is preserved. needs verification.

---

### 4. move 100 files

| question | answer |
|----------|--------|
| who said this? | the handoff blueprint |
| why? | complete BrainArch1 implementation |
| evidence? | handoff provides file inventory by category |
| what if we didn't move all? | incomplete ejection, broken imports, non-functional brain |
| simpler way? | no, partial ejection would break functionality |

**verdict**: requirement is sound. complete ejection required for functional completeness.

---

## questions surfaced during review

### 1. api contract accuracy

in the vision i wrote:

```ts
const brain = genBrainArch1({ slug: 'bhrain/arch1' });
const result = await brain.ask({ prompt: '...', sandbox: 'read-only' });
```

**question**: is this the actual BrainArch1 api? i haven't verified the source.

**risk**: if api differs, the vision misrepresents the user experience.

**mitigation**: verify api in rhachet-roles-bhrain source before proceeding to criteria.

---

### 2. episode continuation claims

the vision mentions episode continuation:

```ts
const step2 = await brain.ask({
  prompt: 'now suggest improvements',
  on: { episode: step1.episode }
});
```

**however**: the brief `define.episode-continuation-limits.md` says:
- atom (sonnet, opus): supported
- atom (haiku): not supported
- repl (all): not supported

**question**: if BrainArch1 is a repl, does it support continuation or not?

**risk**: vision claims functionality that doesn't exist.

**mitigation**: verify BrainArch1 continuation support. if not supported, update vision.

---

### 3. hooks adapter preservation

current repo has:
- `src/domain.operations/hooks/genBrainHooksAdapterForClaudeCode.ts`
- related test files

handoff inventory doesn't explicitly list hooks.

**question**: does BrainArch1 include hooks adapter, or should we preserve the current one separately?

**risk**: losing hooks functionality breaks claude code integration.

**mitigation**: verify hooks coverage in BrainArch1 OR keep current hooks alongside ejected code.

---

### 4. dependency conflicts

**question**: does BrainArch1 require packages not in this repo's deps? will there be version conflicts?

**risk**: post-ejection build failures due to missing or conflicting dependencies.

**mitigation**: compare dependencies before executing ejection.

---

## scope assessment

| dimension | assessment |
|-----------|------------|
| too large? | no — the scope is well-defined (100 files, specific directories) |
| too small? | no — includes all necessary components for functional brain |
| misdirected? | possible — need to verify api contract matches vision |

---

## what i'm now uncertain about

1. **actual BrainArch1 api** — i should verify the contract before finalizing vision
2. **episode continuation** — need to confirm if BrainArch1 supports this
3. **hooks coverage** — unclear if hooks come with BrainArch1 or need preservation

---

## recommendation

**proceed with caution**: the core ejection requirement is sound, but 3 technical questions need verification before criteria phase:

1. verify BrainArch1 api matches vision examples
2. verify episode continuation support
3. verify hooks adapter coverage

these don't block the vision but should inform criteria.

---

🐢 questioned with care, ready to proceed

